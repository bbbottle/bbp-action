{
  "name": "release-bbp-action",
  "version": "1.0.2",
  "description": "",
  "main": "src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "ncc build src/index.js",
    "pub": "git add . && git commit -m 'build' && npm version patch && npm run build  && git tag -a v%npm_package_version% -m 'v$npm_package_version' && git push --follow-tags"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@actions/artifact": "^2.2.1",
    "@actions/core": "^1.11.1",
    "ali-oss": "^6.17.1",
    "@actions/github": "^6.0.0",
    "@vercel/ncc": "^0.38.3"
  },
  "devDependencies": {
    "husky": "^9.1.7"
  }
}
